/*
 * Please do not edit this file.
 * It was generated using rpcgen.
 */

#include "A3.h"
#include <stdio.h>
#include <stdlib.h>
#include <rpc/pmap_clnt.h>
#include <string.h>
#include <memory.h>
#include <sys/socket.h>
#include <netinet/in.h>

#ifndef SIG_PF
#define SIG_PF void(*)(int)
#endif

static void
a3_1(struct svc_req *rqstp, register SVCXPRT *transp)
{
	union {
		listValues mergesort_1_arg;
		newString encryptedecho_1_arg;
		complexValues addcomplex_1_arg;
	} argument;
	char *result;
	xdrproc_t _xdr_argument, _xdr_result;
	char *(*local)(char *, struct svc_req *);

	switch (rqstp->rq_proc) {
	case NULLPROC:
		(void) svc_sendreply (transp, (xdrproc_t) xdr_void, (char *)NULL);
		return;

	case hostname:
		_xdr_argument = (xdrproc_t) xdr_void;
		_xdr_result = (xdrproc_t) xdr_newString;
		local = (char *(*)(char *, struct svc_req *)) hostname_1_svc;
		break;

	case mergeSort:
		_xdr_argument = (xdrproc_t) xdr_listValues;
		_xdr_result = (xdrproc_t) xdr_newList;
		local = (char *(*)(char *, struct svc_req *)) mergesort_1_svc;
		break;

	case encryptedEcho:
		_xdr_argument = (xdrproc_t) xdr_newString;
		_xdr_result = (xdrproc_t) xdr_newString;
		local = (char *(*)(char *, struct svc_req *)) encryptedecho_1_svc;
		break;

	case listFiles:
		_xdr_argument = (xdrproc_t) xdr_void;
		_xdr_result = (xdrproc_t) xdr_newString;
		local = (char *(*)(char *, struct svc_req *)) listfiles_1_svc;
		break;

	case addComplex:
		_xdr_argument = (xdrproc_t) xdr_complexValues;
		_xdr_result = (xdrproc_t) xdr_newComplex;
		local = (char *(*)(char *, struct svc_req *)) addcomplex_1_svc;
		break;

	default:
		svcerr_noproc (transp);
		return;
	}
	memset ((char *)&argument, 0, sizeof (argument));
	if (!svc_getargs (transp, (xdrproc_t) _xdr_argument, (caddr_t) &argument)) {
		svcerr_decode (transp);
		return;
	}
	result = (*local)((char *)&argument, rqstp);
	if (result != NULL && !svc_sendreply(transp, (xdrproc_t) _xdr_result, result)) {
		svcerr_systemerr (transp);
	}
	if (!svc_freeargs (transp, (xdrproc_t) _xdr_argument, (caddr_t) &argument)) {
		fprintf (stderr, "%s", "unable to free arguments");
		exit (1);
	}
	return;
}

int
main (int argc, char **argv)
{
	register SVCXPRT *transp;

	pmap_unset (A3, A3PROG);

	transp = svcudp_create(RPC_ANYSOCK);
	if (transp == NULL) {
		fprintf (stderr, "%s", "cannot create udp service.");
		exit(1);
	}
	if (!svc_register(transp, A3, A3PROG, a3_1, IPPROTO_UDP)) {
		fprintf (stderr, "%s", "unable to register (A3, A3PROG, udp).");
		exit(1);
	}

	transp = svctcp_create(RPC_ANYSOCK, 0, 0);
	if (transp == NULL) {
		fprintf (stderr, "%s", "cannot create tcp service.");
		exit(1);
	}
	if (!svc_register(transp, A3, A3PROG, a3_1, IPPROTO_TCP)) {
		fprintf (stderr, "%s", "unable to register (A3, A3PROG, tcp).");
		exit(1);
	}

	svc_run ();
	fprintf (stderr, "%s", "svc_run returned");
	exit (1);
	/* NOTREACHED */
}
